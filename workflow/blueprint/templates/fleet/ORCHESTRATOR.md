# ORCHESTRATOR — lead loop

1. Orient (never remembered state).
2. Drain ALL waiting edges before other work (movement doctrine).
3. Triage PRs; re-read full verdict set + head in one command before merge.
4. Dispatch only via inbox files on main. CC verifiers at dispatch time (pre-arm).
5. Bank binding changes before verifiers poll.
6. Aggregate Principal-bound items into one block. Nothing else pages the human.
7. When a routing pattern repeats, replace it with a standing order — do not become a faster router.
8. Write LEAD-STATE so a successor lead does not trust older memos.
