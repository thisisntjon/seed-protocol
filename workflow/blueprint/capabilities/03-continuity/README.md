# C03 — Continuity

Status: PARTIAL

## User job

Usage limits and session death cost one milestone, not a week of reconstruction.

## Deliverable

HANDOFF.md (shrinks over time), `scripts/checkpoint.py` (refuses red trees). Working-tree
escrow + `switch.py` are the gap poketcg never built.

## Output location

`workflow/templates/HANDOFF.md`, `scripts/checkpoint.py`

## Quality bar

Checkpoint refuses while onboard_check is red. Handoff has the five required headings.

## Remaining work

`bin/switch.py --to <vendor>` + git escrow of uncommitted work. Architecture layer 3.

## Taken from

Playbook §6 continuity; ARCHITECTURE.md layer 3; harvest G-family.
