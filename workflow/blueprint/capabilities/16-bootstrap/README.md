# C16 — Greenfield bootstrap

Status: SHIPS

## User job

Day one of a new project is one command, not a ritual.

## Deliverable

`python workflow/blueprint/bin/bootstrap.py --dest <clean-git>`

Wraps harvest assemble (CORE + identity templates) and copies this blueprint catalog.

## Output location

`workflow/blueprint/bin/bootstrap.py`, `workflow/harvest/pack/assemble.py`
