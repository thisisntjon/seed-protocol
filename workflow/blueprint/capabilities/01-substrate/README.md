# C01 — Substrate

Status: SHIPS

## User job

Survive session death. A successor reads files, not a chat.

## Deliverable

START-HERE (no later-sentence correction), PLAN, GATES, DECISIONS, RETRACTIONS, templates.
`python scripts/onboard_check.py` exits 0.

## Output location

Repo root identity files + `workflow/templates/` + `scripts/onboard_check.py`.

## Quality bar

Checker fails if a claimed path is missing or a retracted token is re-cited.

## Remaining work

Wake-pack inventories (DOC-ROLES) are CANDIDATE until a repo grows past one START-HERE.

## Taken from

Playbook durable-state-files; SEED Law 5–6; DOC-ROLES; harvest I-A01–A12.
